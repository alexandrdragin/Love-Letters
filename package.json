{
  "name": "love-letters",
  "version": "1.2.1",
  "private": true,
  "description": "A local Chrome extension that hides or replaces messages from selected users on Discord and GitHub.",
  "scripts": {
    "check": "node --check src/core.js && node --check src/content.js && node --check src/popup.js",
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
